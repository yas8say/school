{
  "name": "school",
  "version": "1.0.0",
  "description": "School",
  "main": "index.js",
  "scripts": {
    "postinstall": "yarn setup:install && yarn alummah:install",
    "dev": "yarn setup:dev & yarn alummah:dev",
    "build": "yarn setup:build && yarn alummah:build",

    "setup:install": "cd setup && yarn install",
    "setup:dev": "cd setup && yarn dev",
    "setup:build": "cd setup && yarn build",

    "alummah:install": "cd alummah && yarn install",
    "alummah:dev": "cd alummah && yarn dev",
    "alummah:build": "cd alummah && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
