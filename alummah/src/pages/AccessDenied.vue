<template>
  <div class="min-h-screen bg-gray-50 flex items-center justify-center p-4">
    <div class="text-center">
      <div class="w-24 h-24 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
        <svg class="w-12 h-12 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.35 16.5c-.77.833.192 2.5 1.732 2.5z" />
        </svg>
      </div>
      <h1 class="text-2xl font-bold text-gray-900 mb-2">Access Denied</h1>
      <p class="text-gray-600 mb-6">You don't have permission to access this page.</p>
      <div class="space-x-4">
        <Button @click="$router.back()" variant="outline">Go Back</Button>
        <Button @click="$router.push('/')" variant="solid">Go to Home</Button>
        <Button @click="handleLogout" variant="subtle">Logout</Button>
      </div>
    </div>
  </div>
</template>

<script setup>
import { session } from '@/data/session'

const handleLogout = () => {
  session.logout.submit()
}
</script>